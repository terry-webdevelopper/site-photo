{% extends 'base.html.twig' %}

{% block title %}ADMIN{% endblock %}

{% block body %}
    <h1 class="utilisateur">Compte Admin</h1>

    <section class="body_accueil">
        <article class="art-admin">

        <style>
          .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
          }
          @media (min-width: 768px) {
            .bd-placeholder-img-lg {
              font-size: 3.5rem;
            }
          }
        </style>

    
    <!-- Custom styles for this template -->
    <link href="dashboard.css" rel="stylesheet">

<div class="container-fluid">
  <div class="row">
    <div class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
      <h2 class="titre-tb-users">Mes utilisateurs</h2>
      <div class="table-responsive">
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th scope="col">ID</th>
              <th scope="col">email</th>
              <th scope="col">Noms</th>
              <th scope="col">Prénoms</th>
              <th scope="col">Profils</th>
            </tr>
          </thead>
		  {% for user in listeUser %}
			<tbody>
          <tr>
          <td>{{user.id}}</td>
          <td>{{user.email}}</td>
          <td>{{user.nom}}</td>
          <td>{{user.prenom}}</td>
          {# <td><img src="../assets/uploads/profil.png" alt="{{user.nom}}" width="30"></td> #}
          <td><a href="{{path('profil', {id: user.id})}}">aller sur le profil</a></td>
				</tr>    
          	</tbody>	
		  {% endfor %}
          
        </table>
      </div>
  </div>
</div>

        </article>
    </div>

	

</section>
{% endblock %}
